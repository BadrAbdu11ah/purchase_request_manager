import 'package:flutter/material.dart';

class CreateOrder extends StatelessWidget {
  const CreateOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('إنشاء طلب جديد'), centerTitle: true),
      body: const Center(child: Text('واجهة إنشاء الطلبات قيد التطوير...')),
    );
  }
}
